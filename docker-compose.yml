version: '3.8'

services:
  cc:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - cc_data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  cc_data:
